/**
 * Moment Predictor — базовый URL API (необязательно).
 *
 * Локально: не задавайте MP_API_BASE_URL — в index.html используется http://127.0.0.1:8000
 *
 * Примеры:
 *   window.MP_API_BASE_URL = "http://127.0.0.1:8119";
 *   window.MP_API_BASE_URL = "";  // тот же хост, пути /api и /health проксируются nginx (Docker)
 */
